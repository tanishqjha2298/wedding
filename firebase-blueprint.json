{
  "entities": {
    "rsvp": {
      "title": "RSVP",
      "description": "Guest RSVP response for the wedding",
      "type": "object",
      "properties": {
        "fullName": {
          "type": "string",
          "description": "Full name of the guests"
        },
        "attendance": {
          "type": "string",
          "enum": ["YES", "NO", "VISA"],
          "description": "Attendance choice"
        },
        "selectedEvents": {
          "type": "object",
          "description": "Map of event IDs to attending status (true/false)"
        },
        "songRequest": {
          "type": "string",
          "description": "Optional traditional or Bollywood song request"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The time the RSVP was submitted"
        }
      },
      "required": ["fullName", "attendance", "selectedEvents", "createdAt"]
    }
  },
  "firestore": {
    "/rsvps/{rsvpId}": {
      "schema": "rsvp",
      "description": "Collection storing all guest RSVPs securely"
    }
  }
}
